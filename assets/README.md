# Assets

Upload personal images here when you are ready to replace sample images.

Use simple filenames, for example:

- `window-study-01.jpg`
- `portrait-series-2026.png`

Then reference them in `src/content.js`:

```js
image: "assets/window-study-01.jpg"
```
